[tool.poetry]
name = "app"
version = "0.1.0"
description = "Celery installation"
authors = ["Tim"]
readme = "README.md"

[tool.poetry.dependencies]
python = "^3.10"
requests = "^2.31.0"
celery = "^5.3.6"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
